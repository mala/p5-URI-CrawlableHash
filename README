This is Perl module URI::CrawlableHash.

INSTALLATION

URI::CrawlableHash installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan URI::CrawlableHash

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

URI::CrawlableHash documentation is available as in POD. So you can do:

    % perldoc URI::CrawlableHash

to read the documentation online with your favorite pager.

mala
